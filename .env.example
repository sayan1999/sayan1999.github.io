# Public site URL — used for OG meta tags, canonical, sitemap, and robots.txt.
# Set this to your deployment domain. No trailing slash.
# The predeploy script sets this automatically for GitHub Pages (see package.json).
# Override here for custom domains or other hosts.
VITE_SITE_URL=https://your-domain.com
